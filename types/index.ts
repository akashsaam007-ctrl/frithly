export type BrandVoice = "casual" | "direct" | "professional";

export type CustomerStatus =
  | "active"
  | "cancelled"
  | "churned"
  | "paused"
  | "pending";

export type PlanId = "design_partner" | "starter" | "growth" | "scale";
